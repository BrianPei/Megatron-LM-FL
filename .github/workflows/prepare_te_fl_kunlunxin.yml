name: Prepare TE-FL Kunlunxin Wheel From Config

on:
  workflow_call:
    outputs:
      cache_key:
        value: ${{ jobs.prepare.outputs.cache_key }}

jobs:
  resolve:
    name: resolve-kunlunxin-config
    runs-on: ubuntu-latest
    outputs:
      image: ${{ steps.config.outputs.image }}
      runs_on: ${{ steps.config.outputs.runs_on }}
      container_volumes: ${{ steps.config.outputs.container_volumes }}
      container_options: ${{ steps.config.outputs.container_options }}
      setup_script: ${{ steps.config.outputs.setup_script }}
      nproc_per_node: ${{ steps.config.outputs.nproc_per_node }}
      build_environment: ${{ steps.config.outputs.build_environment }}
    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Install yq
        run: |
          set -euo pipefail
          if ! command -v yq >/dev/null 2>&1; then
            sudo wget -qO /usr/local/bin/yq \
              https://github.com/mikefarah/yq/releases/download/v4.45.1/yq_linux_amd64
            sudo chmod 0755 /usr/local/bin/yq
          fi
          yq --version

      - name: Read Kunlunxin build configuration
        id: config
        run: |
          set -euo pipefail

          config_file=.github/configs/kunlunxin.yml
          image=$(yq -r '.ci_image // ""' "$config_file")
          runs_on=$(yq -o=json -I=0 '.runner_labels // []' "$config_file")
          volumes=$(yq -o=json -I=0 '.container_volumes // []' "$config_file")
          options=$(yq -r '.container_options // ""' "$config_file")
          setup_script=$(yq -r '.setup_script // ""' "$config_file")
          nproc_per_node=$(yq -r '.test_matrix.unit.nproc_per_node // ""' "$config_file")
          build=$(yq -r '.te_fl.build // false' "$config_file")
          build_environment=$(yq -o=json -I=0 '.te_fl.build_environment // {}' "$config_file")

          if [ -z "$image" ] || [ "$runs_on" = '[]' ] || [ -z "$setup_script" ]; then
            echo "::error::Kunlunxin build configuration is incomplete" >&2
            exit 1
          fi
          if [ "$build" != true ]; then
            echo "::error::Kunlunxin configuration must enable TE-FL build" >&2
            exit 1
          fi
          if ! [[ "$nproc_per_node" =~ ^[1-9][0-9]*$ ]]; then
            echo "::error::Kunlunxin nproc_per_node must be a positive integer" >&2
            exit 1
          fi

          {
            echo "image=$image"
            echo "runs_on=$runs_on"
            echo "container_volumes=$volumes"
            echo "container_options=$options"
            echo "setup_script=$setup_script"
            echo "nproc_per_node=$nproc_per_node"
            echo "build_environment=$build_environment"
          } >> "$GITHUB_OUTPUT"

  prepare:
    name: te-fl-kunlunxin
    needs: resolve
    uses: ./.github/workflows/prepare_te_fl.yml
    with:
      platform: kunlunxin
      image: ${{ needs.resolve.outputs.image }}
      runs_on: ${{ needs.resolve.outputs.runs_on }}
      container_volumes: ${{ needs.resolve.outputs.container_volumes }}
      container_options: ${{ needs.resolve.outputs.container_options }}
      setup_script: ${{ needs.resolve.outputs.setup_script }}
      nproc_per_node: ${{ needs.resolve.outputs.nproc_per_node }}
      build_environment: ${{ needs.resolve.outputs.build_environment }}
